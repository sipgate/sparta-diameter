---
name: Build, test and publish snapshot
on:
  push:
    branches:
      - main
    paths:
      - '**/pom.xml'
      - '**/src/**'
  workflow_dispatch:

jobs:
  pipeline:
    uses: sipgate/workflows/.github/workflows/pipeline-maven-artifact-default.yaml@main
    with:
      maven_docker_tag: 3.9.14-eclipse-temurin-17
      skip_analysis: true
    secrets: inherit
